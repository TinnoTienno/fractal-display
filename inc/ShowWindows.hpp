#ifndef SHOWWINDOWS
#define SHOWWINDOWS

struct ShowWindow{
        bool show_properties_window = true;
        bool show_colorset_window = true;
        bool show_movements_window = true;
        bool show_viewport_window = true;
        bool show_fullscreen = false;
}   ;

#endif